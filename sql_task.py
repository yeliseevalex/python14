"""
# Проєкт: «Онлайн-кінотеатр»

## Мета проєкту

Створіть консольний застосунок на Python з використанням **SQLAlchemy ORM** та **SQLite**.

Застосунок повинен імітувати роботу невеликого онлайн-кінотеатру.

Користувачі можуть:

* реєструватися;
* переглядати фільми;
* додавати фільми до обраного;
* оцінювати фільми;
* оформлювати підписку;
* переглядати історію своїх переглядів.

Адміністратор може:

* додавати фільми;
* редагувати фільми;
* видаляти фільми;
* додавати жанри та акторів;
* переглядати статистику.

---

#  1. Структура бази даних

У базі даних повинні бути такі таблиці:

### `User`

```text
id
username
email
created_at
```

### `Movie`

```text
id
title
description
year
duration
```

### `Genre`

```text
id
name
```

### `Actor`

```text
id
name
birth_year
```

### `Rating`

```text
id
user_id
movie_id
value
```

### `Favorite`

```text
id
user_id
movie_id
```

### `Subscription`

```text
id
user_id
plan
started_at
expires_at
```

### `WatchHistory`

```text
id
user_id
movie_id
watched_at
```

---

# 2. Зв'язки між таблицями

Загальна схема:

```text
                         ┌──────────────┐
                         │     User     │
                         └──────┬───────┘
                                │
              ┌─────────────────┼─────────────────┐
              │                 │                 │
              ▼                 ▼                 ▼
         ┌──────────┐      ┌─────────┐      ┌──────────────┐
         │ Favorite │      │ Rating  │      │ Subscription │
         └────┬─────┘      └────┬────┘      └──────────────┘
              │                 │
              └────────┬────────┘
                       ▼
                  ┌─────────┐
                  │  Movie  │
                  └────┬────┘
                       │
              ┌────────┴─────────┐
              │                  │
              ▼                  ▼
        ┌───────────┐      ┌───────────┐
        │   Genre   │      │   Actor   │
        └───────────┘      └───────────┘

User ───────► WatchHistory ◄────── Movie
```

---

#3. Фільми та жанри

Один фільм може належати до декількох жанрів.

Наприклад:

```text
Interstellar
    │
    ├── Фантастика
    ├── Драма
    └── Пригоди
```

Один жанр також може містити багато фільмів.

Отже, між `Movie` та `Genre` потрібно реалізувати зв'язок:

**Many-to-Many.**

Створіть проміжну таблицю:

```text
movie_genres

movie_id
genre_id
```

---

# 4. Фільми та актори

Один фільм може мати багато акторів:

```text
Interstellar
    │
    ├── Matthew McConaughey
    ├── Anne Hathaway
    └── Jessica Chastain
```

А один актор може грати в багатьох фільмах.

Тому між `Movie` та `Actor` також потрібно реалізувати:

**Many-to-Many.**

Проміжна таблиця:

```text
movie_actors

movie_id
actor_id
```

---

# 5. Оцінювання фільмів

Користувач може поставити фільму оцінку від **1 до 10**.

Наприклад:

```text
Олександр → Interstellar → 10
Анна      → Interstellar → 9
Іван      → Interstellar → 8
```

Необхідно зробити так, щоб один користувач **не міг оцінити один і той самий фільм двічі**.

Також перевіряйте, що оцінка знаходиться в діапазоні:

```text
1 ≤ оцінка ≤ 10
```

---

# 6. Обране

Користувач може додати фільм до обраного:

```python
add_to_favorites(user, movie)
```

Видалити:

```python
remove_from_favorites(user, movie)
```

Отримати всі обрані фільми:

```python
get_favorites(user)
```

Наприклад:

```text
Обрані фільми Олександра:

1. Interstellar
2. Inception
3. The Matrix
```

---

# 7. Підписка

У кінотеатрі є три типи підписки:

```text
Basic
Standard
Premium
```

Наприклад:

```text
Basic    → 5$ / місяць
Standard → 10$ / місяць
Premium  → 15$ / місяць
```

Для кожної підписки потрібно зберігати:

* користувача;
* тип підписки;
* дату початку;
* дату завершення.

Створіть функцію:

```python
is_subscription_active(user)
```

Вона повинна повертати:

```text
True
```

якщо підписка ще активна, або:

```text
False
```

якщо вона завершилася.

---

#  8. Перегляд фільму

Створіть функцію:

```python
watch_movie(user, movie)
```

Алгоритм:

```text
Користувач хоче переглянути фільм
              │
              ▼
     Є активна підписка?
          /        \
        Ні          Так
        │            │
        ▼            ▼
    Відмова      Перегляд
                     │
                     ▼
             WatchHistory
```

Якщо підписка відсутня:

```text
 Для перегляду потрібна активна підписка.
```

Якщо підписка активна:

```text
Приємного перегляду!

Фільм: Interstellar
Користувач: Олександр
```

І до `WatchHistory` додається новий запис.

---

# 9. Статистика

Реалізуйте функцію:

```python
get_popular_movies()
```

Вона повинна визначати найпопулярніші фільми за кількістю переглядів.

Наприклад:

```text
Найпопулярніші фільми:

1. Interstellar — 152 перегляди
2. Inception    — 143 перегляди
3. The Matrix   — 121 перегляд
```

Для цього використайте агрегатну функцію:

```text
COUNT
```

разом із:

```text
GROUP BY
ORDER BY
```

---

# 10. Найкращі фільми

Створіть:

```python
get_top_movies()
```

Вона повинна повертати фільми з найвищим середнім рейтингом.

Наприклад:

```text
Найкращі фільми:

1. Interstellar — 9.4
2. Inception    — 9.1
3. The Matrix   — 8.9
```

Для цього використайте:

```text
AVG
GROUP BY
ORDER BY
```

---

# 11. Улюблені жанри користувача

Створіть:

```python
get_user_favorite_genres(user)
```

Функція повинна визначати, які жанри користувач найчастіше переглядає.

Наприклад:

```text
Улюблені жанри Олександра:

Фантастика — 15 переглядів
Драма      — 10 переглядів
Комедія    — 7 переглядів
```

Тут потрібно використати кілька таблиць і `JOIN`.

---

# 12. Пошук фільмів

Створіть:

```python
search_movies(query)
```

Наприклад:

```python
search_movies("star")
```

може повернути:

```text
Star Wars
A Star Is Born
Star Trek
```

Пошук повинен працювати за частиною назви.

Наприклад:

```python
search_movies("star")
```

повинен знаходити не лише фільм із назвою `"Star"`, а всі назви, які містять це слово.

---

# 13. CRUD для фільмів

Обов'язково реалізуйте повний CRUD.

### Створення

```python
create_movie(...)
```

### Отримання одного фільму

```python
get_movie(movie_id)
```

### Отримання всіх фільмів

```python
get_all_movies()
```

### Оновлення

```python
update_movie(...)
```

### Видалення

```python
delete_movie(movie_id)
```

---

# 14. JOIN-запити

Обов'язково використовуйте `JOIN`.

Наприклад, отримайте список:

```text
Фільм | Жанр | Кількість переглядів
```

Результат:

```text
Interstellar | Фантастика | 152
Interstellar | Драма      | 152
Matrix       | Фантастика | 121
```

---

## Ще один JOIN

Отримайте історію переглядів користувача:

```text
Користувач | Фільм       | Дата перегляду
------------------------------------------------
Олександр  | Interstellar| 28.08.2026
Олександр  | Matrix      | 27.08.2026
Олександр  | Inception   | 25.08.2026
```

---

# 15. Детальна інформація про фільм

Реалізуйте:

```python
get_movie_details(movie_id)
```

Функція повинна сформувати повну інформацію про фільм.

Наприклад:

```text
==================================================
                 INTERSTELLAR
==================================================

Рік: 2014
Тривалість: 169 хвилин

Опис:
Команда дослідників вирушає через червоточину
на пошуки нового дому для людства.

Жанри:
    • Фантастика
    • Драма
    • Пригоди

Актори:
    • Matthew McConaughey
    • Anne Hathaway
    • Jessica Chastain

Середній рейтинг: 9.4

Кількість переглядів: 152

Останні оцінки:
    Олександр — 10
    Анна       — 9
    Іван       — 8

==================================================
```

Для реалізації цього запиту потрібно об'єднати інформацію з декількох таблиць.

---

# 16. Додаткове завдання — рекомендації

Створіть:

```python
recommend_movies(user)
```

Система повинна аналізувати історію переглядів користувача.

Наприклад:

```text
Олександр часто дивиться:

Фантастика
Драма
Пригоди
```

Система знаходить фільми цих жанрів, які користувач **ще не переглядав**.

Результат:

```text
Рекомендовано для вас:

1. Dune
2. Arrival
3. Blade Runner 2049
4. The Martian
```

---

# 17. Додаткове ускладнення

Створіть функцію:

```python
get_user_statistics(user)
```

Вона повинна показувати:

```text
========================================
       СТАТИСТИКА КОРИСТУВАЧА
========================================

Користувач: Олександр

Переглянуто фільмів: 47

Оцінено фільмів: 23

Додано до обраного: 12

Улюблений жанр:
    Фантастика

Найчастіше переглядав:
    Interstellar

Середня оцінка користувача:
    8.7

Поточна підписка:
    Premium

Дата завершення:
    15.09.2026

========================================
```

---

# 18. Загальна архітектура

У результаті структура застосунку може бути такою:

```text
online_cinema/
│
├── database.py
│
├── models/
│   ├── user.py
│   ├── movie.py
│   ├── genre.py
│   ├── actor.py
│   ├── rating.py
│   ├── favorite.py
│   ├── subscription.py
│   └── watch_history.py
│
├── services/
│   ├── movie_service.py
│   ├── user_service.py
│   ├── rating_service.py
│   ├── subscription_service.py
│   └── recommendation_service.py
│
├── queries/
│   ├── movie_queries.py
│   └── statistics_queries.py
│
└── main.py
```

На першому етапі можна зробити **весь проєкт в одному файлі**, а після того, як усе запрацює, винести моделі та логіку в окремі модулі.

---

# Що потрібно використати

У цьому проєкті студент повинен попрактикувати:

```text
SQLAlchemy
│
├── create_engine()
│
├── DeclarativeBase
│
├── mapped_column()
│
├── Mapped[]
│
├── Session
│
├── ForeignKey
│
├── relationship()
│
├── One-to-Many
│
├── Many-to-Many
│
├── CRUD
│
├── select()
│
├── where()
│
├── join()
│
├── order_by()
│
├── group_by()
│
├── func.count()
│
├── func.avg()
│
└── and / or
```

---

## Кінцевий результат

Наприкінці має вийти консольний застосунок приблизно з таким меню:

```text
╔══════════════════════════════════════╗
║             ONLINE CINEMA            ║
╠══════════════════════════════════════╣
║                                      ║
║  1. Переглянути фільми                ║
║  2. Знайти фільм                      ║
║  3. Переглянути інформацію про фільм ║
║  4. Переглянути жанри                 ║
║  5. Додати фільм до обраного          ║
║  6. Оцінити фільм                     ║
║  7. Переглянути фільм                 ║
║  8. Історія переглядів                ║
║  9. Моя статистика                    ║
║ 10. Рекомендації                      ║
║                                      ║
║ ───────── АДМІН ──────────────────── ║
║ 11. Додати фільм                      ║
║ 12. Редагувати фільм                  ║
║ 13. Видалити фільм                    ║
║ 14. Статистика фільмів                ║
║                                      ║
║  0. Вийти                             ║
╚══════════════════════════════════════╝
```
"""